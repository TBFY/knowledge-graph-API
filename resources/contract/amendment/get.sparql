PREFIX ocds: <http://data.tbfy.eu/ontology/ocds#>
PREFIX dct: <http://purl.org/dc/terms/>

SELECT (?id as ?oid) ?tit ?amend
WHERE {
	?id a ocds:Contract .
  OPTIONAL { ?id dct:title ?tit } .
  ?id ocds:hasAmendment ?amend .
}
