[
    #foreach( $amendments in $results )
        {
            #set( $control_coma = false )

            #if ( $amendments.amend )
            #set ( $index = $damendments.amend.lastIndexOf("/") )
            #set ( $index = $index + 1 )
            #set ( $ident = $amendments.amend.substring($index, $damendments.amend.length()) )
              #if ( $control_coma )
                ,
              #end
              "id" : "$ident"
              #set( $control_coma = true )
            #end
            
        }
         #if ( $velocityCount < ${results.size()} )
            ,
         #end
    #end
]
