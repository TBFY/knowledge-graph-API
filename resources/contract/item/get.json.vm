[
    #foreach( $items in $results )
        {
            #set( $control_coma = false )

            #if ( $items.itm )
              #set ( $index = $items.itm.lastIndexOf("/") )
              #set ( $index = $index + 1 )
              #set ( $ident = $items.itm.substring($index, $items.itm.length()) )
              #if ( $control_coma )
                ,
              #end
              "id" : "$ident"
              #set( $control_coma = true )
            #end

            #if ( $items.cl1 )
              #if ( $control_coma )
                ,
              #end
              "classificatonScheme" : "$items.cl1"
              #set( $control_coma = true )
            #end

            #if ( $items.cl2 )
              #if ( $control_coma )
                ,
              #end
              "Classificaton" : "$items.cl2"
              #set( $control_coma = true )
            #end

        }
         #if ( $velocityCount < ${results.size()} )
            ,
         #end
    #end
]
