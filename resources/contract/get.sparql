PREFIX ocds: <http://data.tbfy.eu/ontology/ocds#>

SELECT ?s ?startdate ?enddate
WHERE {
	?s a ocds:Contract .
	?s ocds:hasContractPeriod ?hcp .
  OPTIONAL { ?hcp ocds:startDate ?startdate .
		?hcp ocds:endDate ?enddate } .
}
