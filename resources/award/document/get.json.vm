[
    #foreach( $docs in $results )
        {
            #set( $control_coma = false )

            #if ( $docs.doc )
              #set ( $index = $docs.doc.lastIndexOf("/") )
              #set ( $index = $index + 1 )
              #set ( $idoc = $docs.doc.substring($index, $docs.doc.length()) )
              #if ( $control_coma )
                ,
              #end
              "id" : "$idoc"
              #set( $control_coma = true )
            #end

            #if ( $docs.lan )
              #if ( $control_coma )
                ,
              #end
              "language" : "$docs.lan"
              #set( $control_coma = true )
            #end

            #if ( $docs.url )
              #if ( $control_coma )
                ,
              #end
              "url" : "$docs.url"
              #set( $control_coma = true )
            #end

            #if ( $docs.type )
              #if ( $control_coma )
                ,
              #end
              "type" : "$docs.type"
              #set( $control_coma = true )
            #end

        }
         #if ( $velocityCount < ${results.size()} )
            ,
         #end
    #end
]
