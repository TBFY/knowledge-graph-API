[
    #foreach( $suppliers in $results )
        {
            #set( $control_coma = false )

            #if ( $suppliers.s )
            #set ( $index = $suppliers.s.lastIndexOf("/") )
            #set ( $index = $index + 1 )
            #set ( $org = $suppliers.s.substring($index, $suppliers.s.length()) )
              #if ( $control_coma )
                ,
              #end
              "id" : "$org"
              #set( $control_coma = true )
            #end

        }
         #if ( $velocityCount < ${results.size()} )
            ,
         #end
    #end
]
