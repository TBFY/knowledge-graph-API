[
    #foreach( $suppliers in $results )
        {
            #set( $control_coma = false )

            #if ( $suppliers.sas )
              #set ( $index = $suppliers.sas.lastIndexOf("/") )
              #set ( $index = $index + 1 )
              #set ( $id = $suppliers.sas.substring($index, $suppliers.sas.length()) )

              #if ( $control_coma )
                ,
              #end
              "id" : "$id"
              #set( $control_coma = true )
            #end

            #if ( $suppliers.s )
              #if ( $control_coma )
                ,
              #end
              "uri" : "$suppliers.s"
              #set( $control_coma = true )
            #end

            #if ( $suppliers.qname )
              #if ( $control_coma )
                ,
              #end
              "legalName" : "$suppliers.qname"
              #set( $control_coma = true )
            #end

            #if ( $suppliers.qjurisdiction )
              #if ( $control_coma )
                ,
              #end
              "jursidiction" : "$suppliers.qjurisdiction"
              #set( $control_coma = true )
            #end

        }
         #if ( $velocityCount < ${results.size()} )
            ,
         #end
    #end
]
