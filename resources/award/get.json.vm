[
    #foreach( $awards in $results )
        #set( $control_coma = true )
        {
            #if ( $awards.id )
                "id" : "$awards.id",
            #end
            #if ( $awards.tit )
                "title" : "$awards.tit",
            #end
            #if ( $awards.des )
                "description" : "$awards.des"
            #end
        }
         #if ( $velocityCount < ${results.size()} )
            ,
         #end
    #end
]
