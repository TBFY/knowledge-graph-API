{
  #set ( $index = $cpid.lastIndexOf("/") )
  #set ( $index = $index + 1 )
  #set ( $id = $cpid.substring($index, $cpid.length()) )
  #set( $control_coma = false )
  #if ( $id )
    #if ( $control_coma )
      ,
    #end
    "id" : "$id"
    #set( $control_coma = true )
  #end

  #if ( $plan )
    #set ( $indexp = $plan.lastIndexOf("/") )
    #set ( $indexp = $indexp + 1 )
    #set ( $cpplan = $plan.substring($indexp, $plan.length()) )
    #if ( $control_coma )
      ,
    #end
    "plan" : "$cpplan"
    #set( $control_coma = true )
  #end

  #if ( $tender )
    #set ( $indext = $tender.lastIndexOf("/") )
    #set ( $indext = $indext + 1 )
    #set ( $cptender = $tender.substring($indext, $tender.length()) )
    #if ( $control_coma )
      ,
    #end
    "tender" : "$cptender"
    #set( $control_coma = true )
  #end

}
