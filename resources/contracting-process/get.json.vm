[
    #foreach( $contractingp in $results )
        {
            #set( $control_coma = false )
            #set ( $index = $contractingp.s.lastIndexOf("/") )
            #set ( $index = $index + 1 )
            #set ( $id = $contractingp.s.substring($index, $contractingp.s.length()) )
            #if ( $control_coma )
              ,
            #end
            "id" : "$id"
            #set( $control_coma = true )

        }
         #if ( $velocityCount < ${results.size()} )
            ,
         #end
    #end
]
