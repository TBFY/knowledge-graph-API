[
    #foreach( $awards in $results )
        {
            #set( $control_coma = false )

            #if ( $awards.s )
              #if ( $control_coma )
                ,
              #end
              "id" : "$awards.s"
              #set( $control_coma = true )
            #end
        }

         #if ( $velocityCount < ${results.size()} )
            ,
         #end
    #end
]
