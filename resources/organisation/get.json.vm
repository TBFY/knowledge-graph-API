[
    #foreach( $organisation in $results )
        {
            "id" : "$organisation.id",
            "name" : "$organisation.ln",
            "uri" : "$organisation.uri"
         }
         #if ( $velocityCount < ${results.size()} )
            ,
         #end
    #end
]
