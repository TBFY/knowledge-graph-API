[
    #foreach( $organisations in $results )
        {
            #set( $control_coma = false )
            #if ( $organisations.s )
              #set ( $index = $organisations.s.lastIndexOf("/") )
              #set ( $index = $index + 1 )
              #set ( $id = $organisations.s.substring($index, $organisations.s.length()) )
              #if ( $control_coma )
                ,
              #end
              "id" : "$organisations.s"
              #set( $control_coma = true )
            #end

            #if ( $organisations.legalName )
              #if ( $control_coma )
                ,
              #end
              "legalName" : "$organisations.legalName"
              #set( $control_coma = true )
            #end

            #if ( $organisations.jur )
              #if ( $control_coma )
                ,
              #end
              "jurisdiction" : "$organisations.jur"
              #set( $control_coma = true )
            #end

        }
         #if ( $velocityCount < ${results.size()} )
            ,
         #end
    #end
]
