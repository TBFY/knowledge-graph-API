if [ -z "$1" ]
  then
    echo -e "\nVERSION required!\n"
    echo -e "\nPlease call '$0 <version>' to run this command\n"
    exit 1
fi
docker pull cbadenes/r4r:latest
docker run -it --rm  \
    -p 8080:7777  \
    -e "SPARQL_ENDPOINT=http://data.businessgraph.io/repositories/tbfy" \
    -e "RESOURCE_NAMESPACE=http://www.sintef.no/ontology/ocds/" \
    tbfy/kg-api:$1
