if [ -z "$1" ]
  then
    echo -e "\nVERSION required!\n"
    echo -e "\nPlease call '$0 <version>' to run this command\n"
    exit 1
fi
docker run -it --rm  \
    -p 8080:7777  \
    -e "SPARQL_ENDPOINT=http://52.19.213.234:7200/repositories/TBFY" \
    -e "RESOURCE_NAMESPACE=http://www.sintef.no/ontology/ocds/" \
    tbfy/kg-api:$1
